'use strict';

angular.module('gltApp')
        .controller('AuthCtrl', ['$scope','$rootScope',
            function($scope, $rootScope) {
            	$scope.isControllerConnected = 'true';

            }]);
